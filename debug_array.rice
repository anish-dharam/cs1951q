fn test(arr: _) -> _ {
    arr[0]
}

fn second(arr: _) -> _ {
    arr[1]
}

fn main() {
    let numbers: [int] = [1, 2, 3] in
    println(int_to_string(test(numbers)));
    println(int_to_string(second(numbers)))
}
